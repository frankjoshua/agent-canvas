{
  "name": "agent-canvas",
  "version": "0.0.1",
  "type": "module",
  "bin": "./channel/index.ts",
  "scripts": {
    "start": "bun install --no-summary && bun channel/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
